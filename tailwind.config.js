// const defaultTheme = require('tailwindcss/defaultTheme')

/** @type {import('tailwindcss').Config} */
module.exports = {
  theme: {
    extend: {
      // fontFamily: {
      //   sans: ['"Poppins"', 'Cairo', ...defaultTheme.fontFamily.sans],
      //   cairo: ['Cairo', ...defaultTheme.fontFamily.sans],
      // },
    },
  },
}
